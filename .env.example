# HadadaHealth Environment Configuration Template
# Copy this file to .env and fill in your actual values
# DO NOT commit .env file to version control

# =============================================================================
# SECURITY CONFIGURATION (REQUIRED)
# =============================================================================

# Session Secret Key - CRITICAL: Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
# Must be at least 32 characters long for security
SESSION_SECRET_KEY=your-cryptographically-secure-secret-key-here

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================

# Environment: development, staging, production
ENVIRONMENT=development

# Enable debug mode (true/false)
DEBUG=true

# Database path (relative to project root)
DATABASE_PATH=data/bookings.db

# =============================================================================
# EXTERNAL API KEYS
# =============================================================================

# OpenRouter API Key for AI features (optional)
OPENROUTER_API_KEY=your-openrouter-api-key-here

# =============================================================================
# PRODUCTION SECURITY SETTINGS (for production deployment)
# =============================================================================

# When ENVIRONMENT=production, additional security measures are automatically enabled:
# - HTTPS-only cookies
# - Strict same-site policy
# - Enhanced session security

# =============================================================================
# QUICK SETUP INSTRUCTIONS
# =============================================================================

# 1. Copy this file to .env:
#    cp .env.example .env

# 2. Generate a secure session key:
#    python -c "import secrets; print(secrets.token_urlsafe(32))"

# 3. Replace SESSION_SECRET_KEY value with the generated key

# 4. Update other values as needed for your environment

# 5. NEVER commit .env file to version control

# =============================================================================
# SECURITY NOTES
# =============================================================================

# - The SESSION_SECRET_KEY is critical for security - never use default values
# - Each environment (dev/staging/prod) should have its own unique key
# - Rotate keys regularly in production
# - Keep keys secure and limit access
# - Use environment-specific configuration management in production